# Dependencies are declared in pyproject.toml (PyPI package name: slug-ig-crawler; CLI: Slug-Ig-Crawler).
# Browser binaries are not installed by pip; use: Slug-Ig-Crawler bootstrap → cache under ~/.slug/
#
# Local development (editable + tooling + optional video/json5 helpers):
#   pip install -e ".[dev,video,json5]"
#
# Production / minimal install from a checkout:
#   pip install .
#
# Docker builds install via Dockerfile (pip install ".[video]").

-e .[dev,video,json5]
